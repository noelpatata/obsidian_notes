To configure permissions we first need to **enable admin permissions**:
**`Realm Settings -> General -> Admin Permissions (toggle button all the way down in the tab)`**
Create a `Resource`, create `Scope`'s, and create a `Permission`'s, and assign a `Permission` to a `Role` with a `Policy`.
