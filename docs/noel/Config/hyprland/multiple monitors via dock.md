to detect multiple monitors via usb-c we need the `displaylink` package.
installing that package led me to errors due to the inexistence of the evdi module.
i fixed that by updating the system and installing `linux` and `linux-headers`again.