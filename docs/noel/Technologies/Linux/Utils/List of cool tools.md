`fzf` -> Navigate through history dinamically inside terminal. To automatically execute `fzf `when pressing `Ctrl+R` in a terminal. Just add this to `.bashrc/.zshrc`:
``` bash
source /usr/share/fzf/key-bindings.zsh
```
