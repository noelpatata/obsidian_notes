# How to get started
First install the following nugget package:
``` bash
dotnet new install Ardalis.CleanArchitecture.Template
```
Then create just create a new project using this template:
``` bash
dotnet new clean-arch -o Downops.CheckInOps
```